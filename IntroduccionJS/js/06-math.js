// Objeto Math
let resultado;

resultado = Math.PI;
resultado = Math.round(2.2);
resultado = Math.round(2.6);
resultado = Math.ceil(2.1); // Siempre redondea hacia arriba
resultado = Math.floor(2.9); // Siempre redondea hacia abajo
resultado = Math.sqrt(144); // Raíz cuadrada
resultado = Math.abs(-200); // Número absoluto
resultado = Math.min(3, 5, 8, 20); // Indica el número mínimo
resultado = Math.max(1, 2, 45, 9); // Indica el número máximo
resultado = Math.random(); // Númeor aleatorio de 0 a 1
resultado = Math.floor(Math.random() * 30); // Multiplico el random * x valor y lo redondeo para que dé un número entero